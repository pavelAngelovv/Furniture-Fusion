
import { Box } from '@mui/material';
import RegisterForm from './RegisterForm';

const Register = () => {
  return (
    <Box sx={{mt: '7rem'}}>
      <RegisterForm />
    </Box>
  );
};

export default Register;
